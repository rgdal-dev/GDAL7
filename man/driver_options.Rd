% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create.R
\name{driver_options}
\alias{driver_options}
\title{What a driver will accept}
\usage{
driver_options(x, which = c("creation", "open"))
}
\arguments{
\item{x}{A GDALDriver, or a driver's short name such as \code{"GTiff"}.}

\item{which}{\code{"creation"} for the options \code{\link[=gdal_create]{gdal_create()}} and
\code{\link[=gdal_create_copy]{gdal_create_copy()}} take, or \code{"open"} for the ones \code{\link[=gdal_open]{gdal_open()}} does.}
}
\value{
A data frame with one row per option: \code{name}, \code{type},
\code{description}, \code{default}, \code{min}, \code{max}, \code{required}, \code{scope}, and
\code{choices}, a list column holding the allowed values of the options that
have a fixed set and nothing for the rest.
}
\description{
Each driver describes its own creation and open options, in an XML document
GDAL also validates against, so this is read out of GDAL rather than written
down here. \code{type} is GDAL's own vocabulary: \code{"int"}, \code{"float"}, \code{"string"},
\code{"boolean"}, \code{"string-select"} for one of a fixed set.
}
\examples{
options <- driver_options("GTiff")
options[options$name \%in\% c("COMPRESS", "TILED", "BLOCKXSIZE"),
         c("name", "type", "default")]

# What COMPRESS will take on this build.
options$choices[[which(options$name == "COMPRESS")]]
}
