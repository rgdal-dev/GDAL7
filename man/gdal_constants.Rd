% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constants.R
\name{gdal_constants}
\alias{gdal_constants}
\title{GDAL's integer constants}
\usage{
gdal_constants(prefix = NULL)
}
\arguments{
\item{prefix}{Optional. Keep only the constants whose names start with this.}
}
\value{
A named integer vector.
}
\description{
The enumerators GDAL declares: data types (\verb{GDT_*}), access modes (\verb{GA_*}),
colour interpretations (\verb{GCI_*}), resampling algorithms (\verb{GRA_*},
\verb{GRIORA_*}), open flags (\verb{OF_*}), error classes (\verb{CE_*}, \verb{CPLE_*}) and the
rest. They are returned as one named vector rather than exported one by one,
so that GDAL7 adds two names to the search path instead of two hundred.
}
\details{
A constant that the GDAL in use is too old to declare is absent from the
vector. See \code{\link[=gdal7_capabilities]{gdal7_capabilities()}} for the same question about bindings.
}
\examples{
head(gdal_constants("GDT_"))
gdal_constants("GA_")
}
