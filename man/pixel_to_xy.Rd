% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-io.R
\name{pixel_to_xy}
\alias{pixel_to_xy}
\alias{xy_to_pixel}
\title{Convert between pixel and georeferenced coordinates}
\usage{
pixel_to_xy(geotransform, pixel, line)

xy_to_pixel(geotransform, x, y)
}
\arguments{
\item{geotransform}{A numeric vector of length 6, as a dataset's
\code{geotransform} property returns.}

\item{pixel, line}{Numeric vectors of the same length. Fractional positions
are meaningful: whole numbers fall on pixel corners, so the centre of the
first pixel is \verb{pixel = 0.5, line = 0.5}.}

\item{x, y}{Numeric vectors of the same length, in the dataset's coordinate
reference system.}
}
\value{
\code{pixel_to_xy()} a list of \code{x} and \code{y}. \code{xy_to_pixel()} a list of
\code{pixel} and \code{line}.
}
\description{
\code{pixel_to_xy()} takes pixel and line positions to georeferenced ones, and
\code{xy_to_pixel()} takes them back again.
}
\details{
Both are vectorised, so a whole set of positions is one call.
}
\examples{
ds <- gdal_open(system.file("extdata/test.tif", package = "GDAL7"))
gt <- ds@geotransform

# The centre of the first pixel, and back again.
pixel_to_xy(gt, 0.5, 0.5)
xy_to_pixel(gt, -180 + 9, 90 - 9)

gdal_close(ds)
}
