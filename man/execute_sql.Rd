% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vector.R
\name{execute_sql}
\alias{execute_sql}
\title{Run an SQL statement against a dataset}
\usage{
execute_sql(x, sql, dialect = NULL)
}
\arguments{
\item{x}{A GDALDataset object.}

\item{sql}{The statement.}

\item{dialect}{The SQL dialect: \code{NULL} for the driver's own, \code{"SQLITE"} for
GDAL's SQLite dialect, \code{"OGRSQL"} for GDAL's built-in one.}
}
\value{
A GDALLayer holding the result set, or \code{NULL} for a statement that
returns none. The result set is released when the layer is garbage
collected, or when the dataset is closed.
}
\description{
Run an SQL statement against a dataset
}
\examples{
ds <- gdal_open(system.file("extdata/test.gpkg", package = "GDAL7"))
read_vector(execute_sql(ds, "SELECT name FROM places WHERE population > 1e6"))
gdal_close(ds)
}
