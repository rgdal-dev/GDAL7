% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-io.R
\name{crs_to_wkt}
\alias{crs_to_wkt}
\title{A coordinate reference system as WKT}
\usage{
crs_to_wkt(
  crs,
  format = c("WKT2", "WKT2_2019", "WKT2_2015", "WKT1"),
  multiline = FALSE
)
}
\arguments{
\item{crs}{A coordinate reference system, in any form GDAL reads.}

\item{format}{\code{"WKT2"}, or \code{"WKT2_2015"} or \code{"WKT2_2019"} for a particular
edition, or \code{"WKT1"}.}

\item{multiline}{Whether to lay it out over several lines, which is how to
read one rather than how to store it.}
}
\value{
A WKT string.
}
\description{
The conversion \code{\link[=set_crs]{set_crs()}} does, on its own, for checking a CRS string or
for seeing what one says.
}
\details{
WKT2 is the default because WKT1 cannot carry everything a modern CRS
holds: a projection WKT2 names exactly comes back out of WKT1 as
\code{PROJCS["unknown"]}. WKT1 is still what GDAL writes into a file and what
\code{\link[=get_projection]{get_projection()}} returns, so it is here for comparing against those.
}
\examples{
substr(crs_to_wkt("EPSG:4326"), 1, 30)
substr(crs_to_wkt("EPSG:4326", "WKT1"), 1, 30)

cat(crs_to_wkt("EPSG:3857", multiline = TRUE))
}
