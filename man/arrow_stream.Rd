% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vector.R
\name{arrow_stream}
\alias{arrow_stream}
\title{The Arrow stream of a layer}
\usage{
arrow_stream(x, options = NULL)
}
\arguments{
\item{x}{A GDALLayer object.}

\item{options}{Character vector of \code{KEY=VALUE} options for
\code{OGR_L_GetArrowStream}, for instance \code{"MAX_FEATURES_IN_BATCH=1000"} or
\code{"GEOMETRY_ENCODING=WKB"}.}
}
\value{
A \code{nanoarrow_array_stream}.
}
\description{
The layer as an ArrowArrayStream, which anything that speaks Arrow can
consume: nanoarrow, arrow, duckdb. \code{\link[=read_vector]{read_vector()}} is this plus a conversion
to a data frame.
}
\details{
The stream holds the layer open, and the layer holds its dataset open, so a
stream stays valid until it is released or the dataset is closed by hand.

A layer allows one stream at a time. A stream is released when it is
collected, or at once by \code{\link[=release_arrow_stream]{release_arrow_stream()}}; \code{\link[=read_vector]{read_vector()}} does that
for you.
}
\examples{
ds <- gdal_open(system.file("extdata/test.gpkg", package = "GDAL7"))
stream <- arrow_stream(get_layer(ds, 1))
nanoarrow::infer_nanoarrow_schema(stream)
gdal_close(ds)
}
