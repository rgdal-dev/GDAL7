% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vsi.R
\name{vfs_read_file}
\alias{vfs_read_file}
\alias{vfs_write_file}
\title{Read and write bytes over a GDAL virtual file system}
\usage{
vfs_read_file(path)

vfs_write_file(path, bytes)
}
\arguments{
\item{path}{A VSI path.}

\item{bytes}{A raw vector.}
}
\value{
\code{vfs_read_file()} a raw vector. \code{vfs_write_file()} nothing,
invisibly.
}
\description{
With \verb{/vsimem/} these are how a dataset built in memory leaves GDAL without
ever reaching the disk: create it at a \verb{/vsimem/} path, close it, and read
the bytes.
}
\examples{
# A GeoTIFF that is never written to disk.
ds <- gdal_create("/vsimem/small.tif", 4, 4, bands = 1, type = "Byte")
write_raster(ds, list(as.double(seq_len(16))))
gdal_close(ds)

bytes <- vfs_read_file("/vsimem/small.tif")
length(bytes)
rawToChar(bytes[1:2])

vfs_unlink("/vsimem/small.tif")
}
