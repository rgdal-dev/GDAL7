% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vsi.R
\name{vsi_list}
\alias{vsi_list}
\alias{vsi_stat}
\alias{vsi_exists}
\title{GDAL's virtual file systems}
\usage{
vsi_list(path, limit = -1L)

vsi_stat(path)

vsi_exists(path)
}
\arguments{
\item{path}{A VSI path.}

\item{limit}{Roughly the most entries to list. -1, the default, is all of
them; a limit is what makes listing a large remote prefix bearable. GDAL
reads a directory in chunks and stops at the first chunk past the limit,
so a listing can come back a little longer than asked for.}
}
\value{
\code{vsi_list()} a character vector of names, or NULL. \code{vsi_stat()} a
list of \code{size}, \code{is_directory} and \code{modified}, or NULL. \code{vsi_exists()}
TRUE or FALSE.
}
\description{
Every GDAL path works here, not only files on disk. \verb{/vsimem/} is memory,
\verb{/vsizip/} and \verb{/vsitar/} look inside archives, \verb{/vsicurl/} and the cloud
prefixes reach remote storage, and they compose:
\verb{/vsizip//vsicurl/https://example.org/a.zip/b.tif} is a file inside a zip
that is never downloaded whole.
}
\details{
\code{vsi_list()} returns NULL rather than an error for a path that is not a
directory or holds nothing, since a caller cannot otherwise tell the two
apart. \code{vsi_stat()} returns NULL for a path that is not there.
}
\examples{
vsi_exists(system.file("extdata/test.tif", package = "GDAL7"))
vsi_stat(system.file("extdata/test.tif", package = "GDAL7"))$size

# What ships with the package.
vsi_list(system.file("extdata", package = "GDAL7"))
}
