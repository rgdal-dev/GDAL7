% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create.R
\name{gdal_create}
\alias{gdal_create}
\title{Create a new raster dataset}
\usage{
gdal_create(
  dsn,
  xsize,
  ysize,
  bands = 1L,
  type = "Float64",
  driver = "GTiff",
  options = NULL
)
}
\arguments{
\item{dsn}{Where to create it. Any VSI path works, so \code{"/vsimem/x.tif"}
creates it in memory.}

\item{xsize, ysize}{The size in pixels.}

\item{bands}{How many bands. May be 0 for a dataset that carries only
metadata.}

\item{type}{A GDAL data type name, such as \code{"Byte"}, \code{"Int16"} or
\code{"Float32"}. \code{\link[=gdal_data_types]{gdal_data_types()}} lists them.}

\item{driver}{The driver's short name.}

\item{options}{Creation options, as a named character vector or list, or
already written as \code{"KEY=VALUE"} strings.}
}
\value{
A GDALDataset, open for writing.
}
\description{
An empty raster of a given size and type, open for writing. Fill it with
\code{\link[=write_raster]{write_raster()}}, give it a position with \code{\link[=set_geotransform]{set_geotransform()}} and a
coordinate reference system with \code{\link[=set_crs]{set_crs()}}, and close it with
\code{\link[=gdal_close]{gdal_close()}}, which is what finishes the file.
}
\details{
Creation options are checked against the driver's own option list before
anything is created, so a misspelled option is an error rather than a file
that quietly lacks what was asked for. \code{\link[=driver_options]{driver_options()}} is that list.

Not every driver can do this: many can only copy an existing dataset, which
is \code{\link[=gdal_create_copy]{gdal_create_copy()}}. \code{gdal_drivers()} reports which is which.
}
\examples{
path <- tempfile(fileext = ".tif")
ds <- gdal_create(path, 10, 10, bands = 1, type = "Float32",
                  options = c(COMPRESS = "DEFLATE"))

set_geotransform(ds, c(0, 1, 0, 10, 0, -1))
set_crs(ds, "EPSG:4326")
write_raster(ds, list(as.double(seq_len(100))))

gdal_close(ds)
gdal_info(path)$band_info
unlink(path)
}
