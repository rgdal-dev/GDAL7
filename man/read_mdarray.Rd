% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/z-multidim.R
\name{read_mdarray}
\alias{read_mdarray}
\title{Read from a multidimensional array}
\usage{
read_mdarray(x, start = NULL, count = NULL, step = NULL, nodata_as_na = TRUE)
}
\arguments{
\item{x}{A GDALMDArray object}

\item{start}{Origin of the read, one value per dimension, in the array's own
dimension order and counting from 1. Defaults to the start of each
dimension.}

\item{count}{How many values to read along each dimension. Defaults to as
many as \code{start} and \code{step} allow.}

\item{step}{Distance between values along each dimension, which may be
negative to read backwards. Defaults to 1.}

\item{nodata_as_na}{Whether to turn the array's nodata value into \code{NA}.
TRUE by default.}
}
\value{
A numeric array, or a plain numeric vector for a one-dimensional
array.
}
\description{
Reads a hyperslab: an origin, a count of values along each dimension, and a
step between them. With no arguments it reads the whole array.
}
\details{
The result's \code{dim} is the reverse of the array's own dimension order, so a
\verb{(time, lat, lon)} array reads into an R array indexed \verb{[lon, lat, time]}.
That is the order ncdf4 and RNetCDF use, and it is also the
order the values already arrive in, so nothing is moved to produce it. The
names on \code{dim} say which is which. A one-dimensional array comes back as a
plain vector.
}
\examples{
path <- system.file("extdata/multidim.zarr", package = "GDAL7")
ds <- gdal_open(path, multidim = TRUE)
arr <- open_mdarray(get_root_group(ds), "temperature")

dim(read_mdarray(arr))

# The first time step, every second longitude.
read_mdarray(arr, start = c(1, 1, 1), count = c(1, 4, 3), step = c(1, 1, 2))

gdal_close(ds)
}
