% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-info.R
\name{set_nodata_value}
\alias{set_nodata_value}
\alias{set_scale}
\alias{set_offset}
\alias{set_unit_type}
\alias{set_color_interpretation}
\title{Set what a band's values mean}
\usage{
set_nodata_value(x, value)

set_scale(x, value)

set_offset(x, value)

set_unit_type(x, value)

set_color_interpretation(x, value)
}
\arguments{
\item{x}{A GDALRasterBand of a dataset open for writing.}

\item{value}{The value to set. For \code{set_color_interpretation()}, one of the
names in \code{\link[=gdal_color_interpretations]{gdal_color_interpretations()}}, such as \code{"Red"} or \code{"Alpha"}.}
}
\value{
\code{x}, invisibly.
}
\description{
The properties that give a band's numbers meaning, for a dataset being
built. Each is the setter for the accessor of the same name:
\code{\link[=get_nodata_value]{get_nodata_value()}}, \code{\link[=get_scale]{get_scale()}}, \code{\link[=get_offset]{get_offset()}}, \code{\link[=get_unit_type]{get_unit_type()}},
\code{\link[=get_color_interpretation]{get_color_interpretation()}}.
}
\details{
A nodata value of NULL takes the value off the band, which is a different
thing from setting it to NaN. Scale and offset are how a band of integers
carries real values: the value meant is \code{raw * scale + offset}, which GDAL
records but does not apply, so \code{\link[=read_raster]{read_raster()}} returns the raw numbers.
}
\examples{
path <- tempfile(fileext = ".tif")
ds <- gdal_create(path, 4, 4, bands = 1, type = "Int16")
band <- get_raster_band(ds, 1)

set_nodata_value(band, -999)
set_scale(band, 0.1)
set_offset(band, 20)
set_unit_type(band, "degC")

c(nodata = get_nodata_value(band), scale = get_scale(band),
  offset = get_offset(band))

gdal_close(ds)
unlink(path)
}
