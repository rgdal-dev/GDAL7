% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vector.R
\name{write_vector}
\alias{write_vector}
\title{Write a data frame to a vector dataset}
\usage{
write_vector(
  x,
  dsn,
  layer = NULL,
  driver = "GPKG",
  crs = NULL,
  geometry_type = "Unknown",
  geometry_column = NULL,
  fid_column = NULL,
  dataset_options = NULL,
  layer_options = NULL,
  write_options = NULL
)
}
\arguments{
\item{x}{A data frame. A list column of raw vectors is the geometry.}

\item{dsn}{Where to write it.}

\item{layer}{The layer name to create. Defaults to the file's base name.}

\item{driver}{The GDAL driver's short name.}

\item{crs}{The coordinate reference system, in anything GDAL accepts:
WKT, \code{"EPSG:4326"}, a PROJ string. \code{NULL} for none.}

\item{geometry_type}{The geometry type of the layer, by GDAL's name for it,
for instance \code{"Point"} or \code{"3D Polygon"}. \code{"Unknown"} lets the driver
take whatever arrives.}

\item{geometry_column}{The column holding WKB geometry. The default finds
the one list column of raw vectors, which is what \code{\link[=read_vector]{read_vector()}} returns.}

\item{fid_column}{The column holding feature ids, which is a property of the
layer rather than a field of it. The default uses a column called \code{fid} if
there is one.}

\item{dataset_options, layer_options, write_options}{Character vectors of
\code{KEY=VALUE} options for dataset creation, layer creation and the write.}
}
\value{
\code{dsn}, invisibly.
}
\description{
The other direction of the same Arrow path: the data frame becomes one
record batch, GDAL creates the fields from its schema, and
\code{OGR_L_WriteArrowBatch} writes it.
}
\examples{
places <- read_vector(system.file("extdata/test.gpkg", package = "GDAL7"))
path <- tempfile(fileext = ".gpkg")
write_vector(places, path, layer = "places", crs = "EPSG:4326")
identical(nrow(read_vector(path)), nrow(places))
unlink(path)
}
