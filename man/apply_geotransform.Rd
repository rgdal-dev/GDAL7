% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-io.R
\name{apply_geotransform}
\alias{apply_geotransform}
\alias{inv_geotransform}
\title{Convert between pixel and georeferenced coordinates}
\usage{
apply_geotransform(geotransform, pixel, line)

inv_geotransform(geotransform)
}
\arguments{
\item{geotransform}{A numeric vector of length 6, as a dataset's \code{geotransform} property
returns.}

\item{pixel, line}{Numeric vectors of the same length. Fractional positions
are meaningful: whole numbers fall on pixel corners, so the centre of the
first pixel is \verb{pixel = 0.5, line = 0.5}.}
}
\value{
\code{apply_geotransform()} returns a list of \code{x} and \code{y}.
\code{inv_geotransform()} returns a numeric vector of length 6, or NULL when
the geotransform cannot be inverted.
}
\description{
\code{apply_geotransform()} takes pixel and line positions to georeferenced ones.
\code{inv_geotransform()} returns the geotransform that goes the other way, which
is what to feed back to \code{apply_geotransform()} to turn coordinates into
pixel positions.
}
\details{
Both are vectorised over \code{pixel} and \code{line}, so a whole set of positions is
one call.
}
\examples{
ds <- gdal_open(system.file("extdata/test.tif", package = "GDAL7"))
gt <- ds@geotransform

# The centre of the first pixel, and back again.
apply_geotransform(gt, 0.5, 0.5)
apply_geotransform(inv_geotransform(gt), -180 + 9, 90 - 9)

gdal_close(ds)
}
