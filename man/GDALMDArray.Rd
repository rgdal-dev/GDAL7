% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/z-multidim.R
\name{GDALMDArray}
\alias{GDALMDArray}
\title{GDALMDArray class}
\usage{
GDALMDArray(.ptr = NULL)
}
\arguments{
\item{.ptr}{Internal. External pointer to the underlying GDAL object.}
}
\description{
Represents a multidimensional array in a GDAL dataset. What the array says
about itself is a property of it:
}
\details{
\itemize{
\item \code{name} and \code{full_name}, the path from the root of the dataset.
\item \code{data_type_name}, \code{unit_type}, \code{nodata_value}, \code{scale}, \code{offset}.
\item \code{crs}, as WKT2.
\item \code{dimension_count}, and \code{dimensions}, a data frame with one row per
dimension: its \code{name} and \code{size}, the \code{type} and \code{direction} the format
gave it (both may be empty), and \code{indexed}, whether it has a coordinate
variable. Dimensions are in GDAL's order, slowest varying first, which is
the order a format declares them in. \code{\link[=read_mdarray]{read_mdarray()}} returns its \code{dim}
the other way round; see there for why.
\item \code{attributes}, the format's own annotations: units, long names,
conventions, valid ranges. They come back all at once because reading
them one at a time is what makes inspecting a large NetCDF slow.
\item \code{coordinate_variables}, the arrays a format names as this one's
coordinates. That is not quite the same question as which dimensions are
indexed: a swath carries latitude and longitude arrays that are
two-dimensional and index no dimension at all.
\item \code{dimension_values}, each dimension's coordinate variable read in full, in
the array's own dimension order. For a gridded array this is the time,
latitude and longitude the values are placed at, and it is the shorter
road than \code{coordinate_variables}. A dimension with no coordinate variable
is NULL.
}
}
