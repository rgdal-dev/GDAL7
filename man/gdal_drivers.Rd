% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/driver.R
\name{gdal_drivers}
\alias{gdal_drivers}
\title{Every driver this GDAL has}
\usage{
gdal_drivers(capabilities = NULL)
}
\arguments{
\item{capabilities}{Optional character vector of capabilities every returned
driver must have, named as GDAL names them: \code{"DCAP_RASTER"},
\code{"DCAP_VECTOR"}, \code{"DCAP_MULTIDIM_RASTER"}, \code{"DCAP_CREATE"},
\code{"DCAP_CREATECOPY"}, \code{"DCAP_VIRTUALIO"}.}
}
\value{
A data frame with one row per driver: \code{short_name}, \code{long_name},
\code{raster}, \code{vector}, \code{multidim}, \code{create}, \code{copy}, \code{vsi} and \code{extensions}.
}
\description{
The whole table in one call rather than one call per property per driver,
which on a typical build is the difference between one round trip and about
fourteen hundred.
}
\examples{
drivers <- gdal_drivers()
nrow(drivers)

# The formats that can be written from nothing, rather than only copied.
head(gdal_drivers("DCAP_CREATE")$short_name, 12)
}
