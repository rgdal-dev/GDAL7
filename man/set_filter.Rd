% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vector.R
\name{set_filter}
\alias{set_filter}
\title{Restrict which features a layer returns}
\usage{
set_filter(x, where, bbox)
}
\arguments{
\item{x}{A GDALLayer object.
A clause the driver cannot make sense of is not reported here: most drivers
hand it to their own engine when the layer is next used, so a bad clause
surfaces as a GDAL warning from \code{\link[=feature_count]{feature_count()}} or \code{\link[=read_vector]{read_vector()}}.}

\item{where}{An SQL WHERE clause without the WHERE, or \code{NULL} to clear it.}

\item{bbox}{Numeric of length 4, \code{c(xmin, ymin, xmax, ymax)} in the layer's
own coordinates, or \code{NULL} to clear it.}
}
\value{
The layer, invisibly.
}
\description{
Both filters are set on the layer and stay set. They apply to everything
read afterwards, including \code{\link[=feature_count]{feature_count()}} and \code{\link[=read_vector]{read_vector()}}. Passing
\code{NULL} clears the filter.
}
\details{
A spatial filter is what makes a large layer cheap: a driver with a spatial
index uses it rather than reading every feature.
}
\examples{
ds <- gdal_open(system.file("extdata/test.gpkg", package = "GDAL7"))
layer <- get_layer(ds, 1)
set_filter(layer, where = "population > 1e6")
feature_count(layer)
gdal_close(ds)
}
