% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vector.R
\name{release_arrow_stream}
\alias{release_arrow_stream}
\title{Give an Arrow stream back to its layer}
\usage{
release_arrow_stream(stream)
}
\arguments{
\item{stream}{A stream from \code{\link[=arrow_stream]{arrow_stream()}}.}
}
\value{
\code{NULL}, invisibly. Releasing a stream twice is harmless.
}
\description{
A layer allows one Arrow stream at a time, so a stream taken with
\code{\link[=arrow_stream]{arrow_stream()}} has to be released before the layer can be read again.
Doing nothing releases it too, when R next collects it.
}
\examples{
ds <- gdal_open(system.file("extdata/test.gpkg", package = "GDAL7"))
stream <- arrow_stream(get_layer(ds, 1))
release_arrow_stream(stream)
gdal_close(ds)
}
