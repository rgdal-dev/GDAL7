% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vector.R
\name{read_vector}
\alias{read_vector}
\title{Read a vector layer into a data frame}
\usage{
read_vector(x, layer = 1, where = NULL, bbox = NULL, options = NULL)
}
\arguments{
\item{x}{A GDALLayer object, or a GDALDataset, or a path or connection
string to open.}

\item{layer}{When \code{x} is a dataset or a path: which layer, by name or by
position. Ignored when \code{x} is already a layer.}

\item{where}{An SQL WHERE clause to apply first, or \code{NULL}.}

\item{bbox}{Numeric of length 4, \code{c(xmin, ymin, xmax, ymax)}, to apply
first, or \code{NULL}.}

\item{options}{Character vector of \code{KEY=VALUE} options passed to
\code{OGR_L_GetArrowStream}.}
}
\value{
A data frame.
}
\description{
The whole layer in one call, through GDAL's column-oriented Arrow API rather
than feature by feature. Geometry arrives as WKB, in a list column of raw
vectors, which is what every other R geometry package can read.
}
\examples{
read_vector(system.file("extdata/test.gpkg", package = "GDAL7"))
}
