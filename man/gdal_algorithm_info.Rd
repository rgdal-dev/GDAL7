% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/algorithm.R
\name{gdal_algorithm_info}
\alias{gdal_algorithm_info}
\title{Describe one of GDAL's algorithms}
\usage{
gdal_algorithm_info(path)
}
\arguments{
\item{path}{The algorithm, as for \code{\link[=gdal_algorithms]{gdal_algorithms()}}.}
}
\value{
A list: \code{name}, \code{description}, \code{long_description}, \code{url},
\code{sub_algorithms}, and \code{arguments}, a data frame with one row per argument.
Its \code{choices} column is a list, holding the allowed values of the arguments
that have a fixed set and nothing for the rest.
}
\description{
What the algorithm does, what it is called, and every argument it takes with
its type and whether it is required. This is the reference for what to put in
the \code{args} of \code{\link[=gdal_run]{gdal_run()}}, read out of GDAL itself rather than copied into
this package.
}
\examples{
if (gdal_has_algorithms()) {
  info <- gdal_algorithm_info("raster reproject")
  info$description
  head(info$arguments[c("name", "type", "required")])
}
}
