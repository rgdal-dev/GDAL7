% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/z-multidim.R
\name{as_classic_dataset}
\alias{as_classic_dataset}
\title{See a two-dimensional slice of an array as an ordinary raster}
\usage{
as_classic_dataset(x, x_dim = NULL, y_dim = NULL)
}
\arguments{
\item{x}{A GDALMDArray object}

\item{x_dim, y_dim}{Which dimension is the raster's X and which its Y,
counting from 1 in the array's own dimension order. By default the format's
own horizontal X and Y dimensions are used, falling back to the last two.}
}
\value{
A GDALDataset object.
}
\description{
This is the bridge back to the rest of the package: the result is a
GDALDataset, so \code{\link[=read_raster]{read_raster()}}, \code{\link[=get_geotransform]{get_geotransform()}} and the band accessors
all work on it. The array must have exactly two dimensions; take a
\code{\link[=get_view]{get_view()}} of it first if it has more.
}
