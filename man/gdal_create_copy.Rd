% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create.R
\name{gdal_create_copy}
\alias{gdal_create_copy}
\title{Copy a dataset, in another format or with other options}
\usage{
gdal_create_copy(
  x,
  dsn,
  driver = "GTiff",
  options = NULL,
  strict = FALSE,
  progress = interactive()
)
}
\arguments{
\item{x}{A GDALDataset, or a DSN string to open and close again.}

\item{dsn}{Where to write it. Any VSI path works.}

\item{driver}{The driver's short name.}

\item{options}{Creation options, as for \code{\link[=gdal_create]{gdal_create()}}.}

\item{strict}{Whether the copy must be exact. FALSE, the default, lets the
driver approximate what it cannot represent, which is what makes a copy
into a narrower format possible at all.}

\item{progress}{Whether to draw a progress bar. Interactive sessions do by
default. Ctrl-C stops the copy.}
}
\value{
A GDALDataset, open.
}
\description{
The driver reads the whole of \code{x} and writes it out its own way. This is how
formats that cannot be built pixel by pixel are made, the Cloud Optimized
GeoTIFF among them, and it carries across everything the target format can
hold: bands, type, geotransform, coordinate reference system, nodata and
metadata.
}
\details{
The file is complete once the returned dataset is closed.
}
\examples{
path <- tempfile(fileext = ".tif")
out <- gdal_create_copy(
  system.file("extdata/test.tif", package = "GDAL7"), path,
  driver = "COG", options = c(COMPRESS = "DEFLATE"), progress = FALSE
)
gdal_close(out)

# The GTiff driver reports the layout it found, which is how to tell that
# what was written really is a COG.
ds <- gdal_open(path)
get_metadata_item(ds, "LAYOUT", "IMAGE_STRUCTURE")
gdal_close(ds)
unlink(path)
}
