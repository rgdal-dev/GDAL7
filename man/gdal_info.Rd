% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster-io.R
\name{gdal_info}
\alias{gdal_info}
\title{Summarise a dataset in one call}
\usage{
gdal_info(x)
}
\arguments{
\item{x}{A GDALDataset, or a DSN string to open and close again.}
}
\value{
A list with \code{driver}, \code{driver_long}, \code{size}, \code{bands} (the count),
\code{projection}, \code{geotransform}, \code{files}, \code{metadata}, and \code{band_info}, a data
frame with one row per band.
}
\description{
Everything \code{gdalinfo} reports that does not need a pass over the pixels,
fetched in a single call rather than one per property. Reached one accessor
at a time the same summary is seven round trips per band plus a handful for
the dataset, which over \verb{/vsicurl/} is the difference between immediate and
sluggish.
}
\examples{
info <- gdal_info(system.file("extdata/test.tif", package = "GDAL7"))
info$size
info$band_info
}
