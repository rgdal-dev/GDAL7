% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vsi.R
\name{gdal_config}
\alias{gdal_config}
\alias{gdal_config_options}
\alias{with_gdal_config}
\title{GDAL's configuration options}
\usage{
gdal_config(name, value)

gdal_config_options()

with_gdal_config(options, expr)
}
\arguments{
\item{name}{The option's name.}

\item{value}{Its value, or NULL to unset it. A logical is written as GDAL
writes them, \code{"YES"} or \code{"NO"}.}

\item{options}{A named character vector or list of options to set.}

\item{expr}{The expression to evaluate with them set.}
}
\value{
\code{gdal_config()} reading returns a string or NULL, and setting
returns the previous value invisibly. \code{gdal_config_options()} returns
every option that is set, as a named character vector.
\code{with_gdal_config()} returns what \code{expr} returned.
}
\description{
GDAL reads these at the moment it needs them, so setting one changes how the
next call behaves. They steer caching, credentials, and a good deal of
per-driver behaviour: \code{GDAL_CACHEMAX} is the block cache size,
\code{CPL_VSIL_CURL_ALLOWED_EXTENSIONS} limits what \verb{/vsicurl/} will fetch,
\code{AWS_NO_SIGN_REQUEST} makes an S3 bucket public.
}
\details{
\code{gdal_config()} with a value sets one; with only a name it reads one, and
returns NULL when it is not set, which is not the same as being set to the
empty string. \code{with_gdal_config()} sets options for one expression and puts
back exactly what was there, including unsetting what was unset.
}
\examples{
# Read a raster with GDAL's block cache set small, then leave it as it was.
with_gdal_config(c(GDAL_CACHEMAX = "16"), {
  gdal_config("GDAL_CACHEMAX")
})

gdal_config("GDAL_CACHEMAX")
}
