.Rproj.user
.Rhistory
.Rdata
.httr-oauth
.DS_Store
.quarto

# Compiled code
src/*.o
src/*.so
src/*.dll

# Build artifacts
*.tar.gz

# NOTE: src/cpp11.cpp and R/cpp11.R are deliberately tracked. They are
# generated by cpp11::cpp_register(), but without them a clone cannot be
# installed, which breaks remotes::install_github() and R CMD build.

# Written by ./configure from src/Makevars.in
src/Makevars

# R CMD check output
*.Rcheck/

# GDAL writes a PAM sidecar beside any dataset whose metadata changes.
*.aux.xml

# rmarkdown leaves a preview next to the github_document output.
README.html

# pkgdown builds the site here; it is published from the gh-pages branch.
docs/
